## 버그: 구글 폼 신규 신청 시 텔레그램 알림 없음 (입금 대기)
## 원인:
`run_applicant_watch`가 `load_data_raw(apply_eligibility=True)`로 시트를 읽어 **입금 미확인(paid=False) 행이 필터로 제거**됨 → 신규 행 감지 불가.
## 영향 파일:
- `utils/telegram_notify.py`
## 수정 내용:
- 알림용 시트 로드는 `apply_eligibility=False` (전체 행 기준 행번호 비교)
## 재발 방지:
- `tests/test_telegram_notify.py::test_process_new_applicants_notifies_unpaid_row`
## 검증 명령:
```bash
python -m pytest tests/test_telegram_notify.py -q
```
