## 버그: 로컬/데모 모드에서 「데이터가 없습니다」
## 원인:
`load_demo_data()`가 `normalize_dataframe(apply_eligibility=True)` 기본값 사용.
데모 행에 `paid`(입금확인) 필드 없음 → `COLUMN_DEFAULTS`로 `paid=False` →
`apply_eligibility_filter`가 **전원 제외** → 0건.
## 영향 파일:
- `utils/sheets.py` (`load_demo_data`)
- `app.py` (빈 목록 안내)
## 수정 내용:
- 데모 로드 시 `apply_eligibility=False`
- **빈 df session 캐시** 무효화 (`should_reload_df`, 시작 시 자동 `_refresh_data`)
- 데모↔시트 전환 시 캐시 초기화
- 빈 목록 시 데모/시트/입금필터별 안내 문구
## 재발 방지:
- `tests/test_demo_data.py`
- `tests/test_data_loader.py`
## 검증 명령:
`python -m pytest tests/test_demo_data.py tests/test_data_loader.py -q`
