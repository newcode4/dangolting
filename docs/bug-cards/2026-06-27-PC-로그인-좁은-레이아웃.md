## 버그: PC 로그인 화면이 가운데 좁은 띠로 표시됨
## 원인:
- `st.columns([2.2, 1.6, 2.2])` → 폼이 화면 26%만 사용
- 로그인에서 `st.stop()` → `theme.css` 미로드
## 영향 파일:
- `utils/auth.py`, `app.py`, `assets/theme.css`
## 수정 내용:
- columns 제거, `auth-page-marker` + flex 중앙 정렬 (PC 520px 카드)
- `is_admin_route()` 시 gate 전 theme.css 주입
## 재발 방지:
- `tests/test_auth_cookie.py`, `tests/test_theme_css.py`
## 검증 명령:
- `pytest tests/test_theme_css.py tests/test_auth_cookie.py -q`
