## 버그: PC 관리자 화면이 가운데 좁게 표시됨
## 원인:
- `theme.css` `block-container`에 `max-width:1400px`만 있고 `width:100%` 없음 → 콘텐츠 intrinsic width로 축소
- 모든 `stHorizontalBlock` 첫 열을 260px 고정 → 목록·상세 외 레이아웃도 폭 제한
## 영향 파일:
- `assets/theme.css`
- `app.py`
## 수정 내용:
- PC `block-container` / `stMainBlockContainer` → `width:100%`, `max-width:none`
- 260px 전역 규칙 제거 → `.list-layout-anchor` 2열만 280~380px + 나머지 flex
- theme CSS mtime 주석으로 캐시 무효화
## 재발 방지:
- `tests/test_theme_css.py`: full-width·scoped list layout 회귀 테스트
## 검증 명령:
- `pytest tests/test_theme_css.py -q`
