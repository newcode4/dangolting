## 버그: 시트 로드 KeyError '매칭여부' / matched
## 원인:
구글 폼 시트 헤더가 코드 기대값과 다름 (`매칭여부` vs `매칭 여부`).
또는 시트에 매칭·환불 등 컬럼이 아예 없을 때 `df["matched"]` 접근으로 KeyError.
## 영향 파일:
- `utils/columns.py`, `app.py`, `utils/sheets.py`
## 수정 내용:
- `CN` 헤더 매핑 + `normalize_dataframe`
- `ensure_columns` / `COLUMN_DEFAULTS`로 논리 컬럼 누락 시 기본값 자동 생성
## 재발 방지:
- `tests/test_columns.py`
## 검증 명령:
`python -m pytest tests/test_columns.py -q`
